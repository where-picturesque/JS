<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="#"><%=title%></a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <% if(add){ %>
      <ul class="nav navbar-nav navbar-left">
      
      <%if(hasshop){%>
       <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><%=curshop.shopname%> <span class="caret"></span></a>
          <ul class="dropdown-menu">
            <% for(var i=0;i<shopnames.length;i++){%>
              <li><a href="/shop?shopid=<%=shopnames[i]._id%>"><%= shopnames[i].shopname%></a></li>
            <%}%>
          </ul>
        </li>

       <%if(addgood){%>
        <li><a href="/addgoods/category?shopid=<%=curshop._id%>">添加分类</a></li>
        <li><a href="/addgoods?id=<%=curshop._id%>">添加商品</a></li>
        <%}%>

        <%}%>
         <li><a href="/shop/register">注册店铺</a></li>

      </ul>
      <%}%>
      <%if(isshow){%>
      <ul class="nav navbar-nav navbar-right">
      <% if(showlogin){ %>
        <li><a href="/login">登录</a></li>
        <% } %>
        <% if(showreg){%>
        <li><a href="/register">注册</a></li>
        <%}%>
        <%if(showuser){%>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">欢迎登录，<%=user%> <span class="caret"></span></a>
          <ul class="dropdown-menu"> 
            <li>
            <a href="/">回到首页</a>
            </li>
            <%if(hasshop){%>
            <li>
            <a href="/shop?shopid=<%=curshop._id%>">回到店铺</a>
            </li>
            <%}%>
            <% if(showlogout){ %>
            <li>
              <a href="/logout">注销</a>
            </li>
            <% } %>

          </ul>
          </li>
        <%}%>
        
      </ul>
      <% } %>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>